#include<stdio.h>
#include<math.h>
int main()
{
    int T;
    scanf("%d",&T);
    while(T--)
    {
     int x;
     scanf("%d",&x);
     int r=sqrt(x);
     printf("%d\n",r);
    }
    return 0;
}
